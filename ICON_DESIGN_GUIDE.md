# Omnivium 图标设计指南

## 需要的文件

| 文件 | 尺寸 | 用途 |
|------|------|------|
| `app_icon.png` | 1024×1024 | 主图标（全尺寸，含背景） |
| `app_icon_foreground.png` | 1024×1024 | Android 自适应图标前景（透明背景） |

## 设计要求

### 品牌关键词
- **Omnivium** = Omni（全能）+ Vivium（生命）
- AI 超级应用，连接一切
- 科技感 + 生命力

### 配色方案
- 主色：`#6C5CE7`（紫蓝色，代表 AI/智慧）
- 辅色：`#00D2D3`（青色，代表连接/通信）
- 背景：`#1A1A2E`（深色，与应用主题一致）

### 设计方向建议

1. **字母 O + 电路/节点**
   - 大写 O 字母，内部有连接节点
   - 简洁、现代、辨识度高

2. **无限符号 ∞ + AI 核心**
   - 无限符号变形，中间有发光核心
   - 代表全能 + 智慧

3. **六边形 + 脉冲**
   - 六边形（蜂巢/网络）+ 中心脉冲
   - 代表连接 + 生命力

### 设计规范
- 圆角：图标四角留出安全区（Android 会自动裁圆）
- 前景图标：中央 70% 区域放置主图形，四周留白
- 透明度：前景图标的背景必须完全透明
- 格式：PNG，无压缩，无 Alpha 通道（主图标）

## 生成步骤

1. 设计师提供 1024×1024 的源文件
2. 放到 `assets/icon/app_icon.png` 和 `assets/icon/app_icon_foreground.png`
3. 运行：
   ```powershell
   dart run flutter_launcher_icons
   ```
4. 自动生成 Android/iOS 所有尺寸图标

## 应用内图标

当前使用 Lucide Icons（线性风格），建议保持一致：
- 导航栏：Lucide Icons
- 功能图标：Lucide Icons
- 特殊品牌图标：自定义 SVG

如果需要自定义图标，在 `assets/icons/` 目录下放置 SVG 文件。
