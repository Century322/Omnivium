# Keystore for Android release signing
# Generate with: keytool -genkey -v -keystore upload-keystore.jks -keyalg RSA -keysize 2048 -validity 10000 -alias upload
# Then place the .jks file in this directory.
# Update ../key.properties with your passwords.
